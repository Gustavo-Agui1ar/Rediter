import { StyleSheet } from "react-native";

export const indexStyles = StyleSheet.create({
  footer: {
    flexDirection: "row",
    gap: 14,
    backgroundColor: "#FFF",
    padding: 20,
    justifyContent: "center",
    alignItems: "center",
  },
});
