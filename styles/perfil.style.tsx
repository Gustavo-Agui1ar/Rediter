import { StyleSheet } from "react-native";

export const stylesPerfil = StyleSheet.create({
  imageProfile: {
    width: 100,
    height: 100,
    borderRadius: 60,
    borderWidth: 2,
    borderColor: "#333",
    objectFit: "contain",
  },
});
